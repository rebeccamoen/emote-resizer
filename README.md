# Emote Resizer
Quick and easy way to resize images to Twitch emote and badge sizes online with your browser.

## How do I use this?
You can access the live version of this project at [https://rebeccamoen.github.io/emote-resizer]https://rebeccamoen.github.io/emote-resizer

## Developing
Run `yarn install` to install all dependencies, then run `yarn start` to start the development server.

## Building
Run `yarn build` to build a production version of the app.

## Notes
git restore yarn.lock
rm -rf node_modules
nvm use
yarn install
yarn deploy
git branch -a

yarn build / yarn start